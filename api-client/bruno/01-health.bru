meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{apiRoot}}/health
  body: none
  auth: none
}

tests {
  test("health returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
